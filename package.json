{
  "name": "project-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node src/index.ts -P",
    "build": "rimraf build && tsc --noEmit"
  },
  "author": "kurmangali",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "eslint": "^8.24.0",
    "rimraf": "^3.0.2"
  }
}
